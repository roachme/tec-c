changecom()dnl
define(SHEBANG, `#!/usr/bin/env')dnl
SHEBANG SHELLNAME

function tec()
{
    local tecstatus;
    local pwdfile="PWDFILE"

    _tec "$@"
    tecstatus="$?"

    test -s "$pwdfile" && cd "$(cat "$pwdfile")" || return "$tecstatus"
    return "$tecstatus"
}
